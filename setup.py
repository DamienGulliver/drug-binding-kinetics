from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Curated drug binding kinetics datasets and algorithms to work with them.',
    author='damiengulliver',
    license='',
)
